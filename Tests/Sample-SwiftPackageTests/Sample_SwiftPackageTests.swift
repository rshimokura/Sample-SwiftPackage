import XCTest
@testable import Sample_SwiftPackage

final class Sample_SwiftPackageTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(Sample_SwiftPackage().text, "Hello, World!")
    }
}
